import { Header as MantineHeader, NavLink as MantineNavLink } from "@mantine/core";
import styled from "@emotion/styled";

const HeaderWrapper = styled(MantineHeader)`
  display: flex;
  align-items: center;
`;

const NavLink = styled(MantineNavLink)``;

export { HeaderWrapper, NavLink };
